Added a startup notification that is send out after successful initialization to properly test the whole system with user feedback.

Use the new flag `--disable-start-notification` to disable this behavior.
